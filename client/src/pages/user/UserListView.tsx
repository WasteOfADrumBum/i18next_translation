import React from 'react'
import { Typography } from '@mui/material'

const UserListView: React.FC = () => {
	return (
		<div>
			<Typography variant='h2'>User List View</Typography>
			{/* Add user list content here */}
		</div>
	)
}

export default UserListView
